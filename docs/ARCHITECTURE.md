# Architecture

<!-- ЗАПОЛНИТЬ: архитектурная заметка проекта. Агент читает этот файл на старте
     сессии (Startup Workflow, шаг 3) и перед архитектурными решениями (Escalation).
     Держите актуальной: docs обновляются в том же коммите, что и код. -->

## Overview

<!-- ЗАПОЛНИТЬ: 2–4 предложения — из чего состоит система и как она развёрнута. -->

## Components

<!-- ЗАПОЛНИТЬ: список компонентов/модулей и их ответственность.
     Пример таблицы:

| Компонент | Ответственность | Технология |
|---|---|---|
|  |  |  |
-->

## Boundaries & Layer Model

<!-- ЗАПОЛНИТЬ: модель слоёв и допустимые зависимости (кто кого может вызывать).
     Пример: api → service → repository; domain не зависит ни от кого.
     Нарушения границ — то, что ловит check-arch / ревью. -->

- Allowed dependencies:
- Forbidden dependencies:

## Constraints

<!-- ЗАПОЛНИТЬ: архитектурные инварианты, которые нельзя нарушать
     (например: "никаких прямых SQL из контроллеров", "все таймауты настраиваются"). -->

## Decision Records

Ключевые архитектурные решения (с альтернативами, последствиями и revisit-триггерами) фиксируются как ADR — реестр: [docs/adr/README.md](adr/README.md). Оперативные решения сессий — в [DECISIONS.md](../DECISIONS.md); архитектурно значимые из них переносятся в ADR.

## Related Documents

- [Модель данных (ER)](diagrams/er-data-model.md) — каноническая схема данных и инварианты
- [Контракт API (OpenAPI)](api/openapi.yaml) — внешний HTTP-поверхности
- [Диаграммы C1/C2](diagrams/c4-context.md) — контекст и контейнеры
- [Карта документации](README.md) — приоритеты источников при конфликте
