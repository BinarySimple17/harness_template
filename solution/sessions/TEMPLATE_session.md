# 00N — [Краткое название сессии]

<!-- ЗАПОЛНИТЬ: скопируйте файл под ID сессии (<трек><NN>_session.md, например 001_session.md —
     нумерация ведётся в solution/sessions.md),
     заполните по подсказкам; подсказки-комментарии удалить. Строку в индекс solution/sessions.md
     и в граф зависимостей добавить до старта. Сессия = самодостаточный модуль для автономного
     агента-исполнителя: контекст, правила, задачи с проверками — всё внутри файла.
     Связка с feature_list.json: поле «Фичи» в шапке — фичи, с которыми работает сессия;
     Definition of Done задач берётся из полей behavior/verification этих фич, не придумывается.
     Одна фича может занимать НЕСКОЛЬКО сессий: ровно одна (финальная) «закрывает» её через
     make verify-feature; промежуточные «продолжают» — фича остаётся active, продолжение
     фиксируется в progress.md и «Текущем состоянии» индекса. -->

Этап: [этап/трек] | Владелец: [имя/роль] | Зависимости: [ID закрытых сессий] | Оценка: [дни] | Статус: [ ] не начата · ◐ в работе · [x] ✅ ГГГГ-ММ-ДД
Фичи: [feat-0XX — Name; их dependencies уже в passing]
Роль по фиче: [закрывает feat-0XX — финальная сессия, через make verify-feature | продолжает feat-0XX — промежуточная, фича остаётся active]

## ⚠️ Прочитай перед началом

<!-- ЗАПОЛНИТЬ: кто исполнитель и что уже стоит к моменту старта (закрытые
     сессии, поднятая инфраструктура, флаги/ограничения этой сессии).
     Сюда же — что сознательно НЕ делается в этой сессии (границы скоупа). -->

### Правила для исполнителя

<!-- ЗАПОЛНИТЬ: 2–5 правил СПЕЦИФИЧНЫХ для этой сессии поверх канона S00:
     какие файлы не трогать, каким контрактом пользоваться, какие флаги переключать.
     Общие правила (прогресс, верификация, слои) в S00 — не дублировать. -->

1. [правило]

## Предусловия

<!-- ЗАПОЛНИТЬ: проверяемые условия старта — каждая строка с командой проверки.
     Не выполнено = сессия не начинается, а не «начинается и разбирается по ходу». -->

- [ ] [ID предыдущей сессии] закрыта: [команда-доказательство] → [ожидаемый результат]
- [ ] `make check` зелёный (baseline репозитория)
- [ ] Фичи-зависимости полей «Фичи» уже в state=passing (сверка `feature_list.json`; незакрытая зависимость = сессия не начинается, а переупорядочивается)

## Задачи

<!-- ЗАПОЛНИТЬ: задачи по одной, каждая с ✅ Самопроверкой (команды + ожидаемый вывод).
     Task без самопроверки не закрывается (правило №1 S00). Порядок задач = порядок исполнения. -->

### Task 1: [название]

<!-- Что именно сделать: файлы/пути, ключевые сигнатуры/контракты, ссылки на ADR/OpenAPI/ER. -->

**✅ Самопроверка:**

```bash
[команды]   # [ожидаемый результат]
```

---

### Task 2: [название]

**✅ Самопроверка:**

```bash
[команды]
```

## Критерии приёмки

<!-- ЗАПОЛНИТЬ: полная проверяемая съёмка результата сессии; отмечается [x] по факту. -->

- [ ] [критерий — формулировка, допускающая только да/нет]
- [ ] Фичи закрыты или переданы: сессия-«закрывает» — все свои фичи в state=passing с evidence (`make verify-feature F=<id>` для каждой); сессия-«продолжает» — фича остаётся active с allowlist (`bash scripts/clean-state-check.sh . <id>`), продолжение зафиксировано в `progress.md`

## Ловушки

<!-- ЗАПОЛНИТЬ: известные грабли этого объёма работ (по опыту/ревью/прототипу):
     1. [грабля] — [как обойти]. Каждая ловушка — потенциально часы отладки. -->

1. [грабля — как обойти]
2. verify-feature требует state=active: забытая активация фичи на старте первой сессии по ней → `BLOCKED` на закрытии (состояния не перескакивают); активируйте фичу в первый же день, а не перед коммитом
3. Промежуточная сессия многосессионной фичи: без allowlist (`bash scripts/clean-state-check.sh . <id>`) чистое состояние будет FAIL — фича сознательно остаётся `active`; не потеряйте передачу следующей сессии через `progress.md` и «Текущее состояние» индекса

## Отчёт по сессии (заполняется по факту)

<!-- Отчёт — обязательная часть закрытия: без него статус в шапке и индексе не меняется. -->

- Сделано:
  - [Task: что реально сделано, коммиты, число тестов/ассертов, live-прогон]
- Отклонения от плана:
  - [что сделали не так, как в задаче, и почему; каждое отклонение — осознанное]
- Перенесено в другие сессии:
  - [ID сессии — что перенесено и почему; если в беклог — BL-NN в solution/backlog.md]
