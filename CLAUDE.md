# CLAUDE.md

<!-- ЗАПОЛНИТЬ: не требуется. Файл-роутер: Claude Code читает CLAUDE.md,
     а вся инструкция живёт в AGENTS.md, чтобы не дублировать правила.
     Если у вас нет Claude Code — файл можно удалить. -->

All operating instructions live in [AGENTS.md](AGENTS.md) — read it first, completely.

Startup: run `./init.sh`, read `feature_list.json` and `progress.md` before touching code.
Verification: `make check` (or `./init.sh`) must exit 0 before claiming done.
